PROJECT(demo006)

FIND_PACKAGE(glfw3 REQUIRED)
FIND_PACKAGE(OpenGL REQUIRED)

SET(SOURCE_FILES main.cpp ../../lib/glad.c)

ADD_EXECUTABLE(demo006 ${SOURCE_FILES})

TARGET_LINK_LIBRARIES(demo006 glfw)
TARGET_LINK_LIBRARIES(demo006 OpenGL::GL)