PROJECT(demo04)

FIND_PACKAGE(glfw3 REQUIRED)
FIND_PACKAGE(OpenGL REQUIRED)

SET(SOURCE_FILES main.cpp ../../lib/glad.c)

ADD_EXECUTABLE(demo04 ${SOURCE_FILES})

TARGET_LINK_LIBRARIES(demo04 glfw)
TARGET_LINK_LIBRARIES(demo04 OpenGL::GL)