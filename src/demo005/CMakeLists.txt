PROJECT(demo005)

FIND_PACKAGE(glfw3 REQUIRED)
FIND_PACKAGE(OpenGL REQUIRED)

SET(SOURCE_FILES main.cpp ../../lib/glad.c)

ADD_EXECUTABLE(demo005 ${SOURCE_FILES})

TARGET_LINK_LIBRARIES(demo005 glfw)
TARGET_LINK_LIBRARIES(demo005 OpenGL::GL)