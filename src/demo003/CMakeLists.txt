PROJECT(demo003)

FIND_PACKAGE(glfw3 REQUIRED)
FIND_PACKAGE(OpenGL REQUIRED)

SET(SOURCE_FILES main.cpp ../../lib/glad.c)

ADD_EXECUTABLE(demo003 ${SOURCE_FILES})

TARGET_LINK_LIBRARIES(demo003 glfw)
TARGET_LINK_LIBRARIES(demo003 OpenGL::GL)