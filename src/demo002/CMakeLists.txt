PROJECT(demo002)

FIND_PACKAGE(glfw3 REQUIRED)
FIND_PACKAGE(OpenGL REQUIRED)

SET(SOURCE_FILES main.cpp ../../lib/glad.c)

ADD_EXECUTABLE(demo002 ${SOURCE_FILES})

TARGET_LINK_LIBRARIES(demo002 glfw)
TARGET_LINK_LIBRARIES(demo002 OpenGL::GL)