PROJECT(demo05)

FIND_PACKAGE(glfw3 REQUIRED)
FIND_PACKAGE(OpenGL REQUIRED)

SET(SOURCE_FILES main.cpp ../../lib/glad.c)

ADD_EXECUTABLE(demo05 ${SOURCE_FILES})

TARGET_LINK_LIBRARIES(demo05 glfw)
TARGET_LINK_LIBRARIES(demo05 OpenGL::GL)