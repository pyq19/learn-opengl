PROJECT(demo001)

FIND_PACKAGE(glfw3 REQUIRED)
FIND_PACKAGE(OpenGL REQUIRED)

SET(SOURCE_FILES main.cpp ../../lib/glad.c)

ADD_EXECUTABLE(demo001 ${SOURCE_FILES})

TARGET_LINK_LIBRARIES(demo001 glfw)
TARGET_LINK_LIBRARIES(demo001 OpenGL::GL)