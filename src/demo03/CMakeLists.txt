PROJECT(demo03)

FIND_PACKAGE(glfw3 REQUIRED)
FIND_PACKAGE(OpenGL REQUIRED)

SET(SOURCE_FILES main.cpp ../../lib/glad.c)

ADD_EXECUTABLE(demo03 ${SOURCE_FILES})

TARGET_LINK_LIBRARIES(demo03 glfw)
TARGET_LINK_LIBRARIES(demo03 OpenGL::GL)